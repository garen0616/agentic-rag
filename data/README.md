# Data folder

Place your `{dataset_name}_results.csv` files generated by the `EarningsCallAgenticRag` project into this folder. The backend automatically discovers files matching `*_results.csv` and exposes them via `/api/datasets`.
